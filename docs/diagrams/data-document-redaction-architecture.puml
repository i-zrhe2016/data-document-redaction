@startuml
!theme plain
title data-document-redaction 工作流
skinparam shadowing false

start
:使用者提交原件;
:范围与用途路由;
:盘点格式表面;\nPDF / Office / 图片 / 数据;
:检测敏感内容;
:选择并执行最小变换;\n生成副本，原件不覆盖;
:验证可见、隐藏和元数据表面;
:结构、完整性与业务效用校验;

if (关键检查均通过?) then (是)
  :输出 pass\n审查报告;
else (否)
  if (关键表面无法读取?) then (是)
    :输出 blocked\n审查报告;
  else (否)
    :输出 needs_review\n审查报告;
  endif
endif

stop

@enduml
