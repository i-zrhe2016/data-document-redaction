@startuml
!theme plain
title data-document-redaction 工作流
skinparam shadowing false

start
:使用者提交原件或已有候选副本;
:范围与用途路由;

if (已有候选副本?) then (是)
  :锁定版本、验收标准和首轮报告;
else (否)
  :本地生成脱敏副本;\n原件不覆盖;
endif

:盘点格式表面;\nPDF / Office / 图片 / 数据;
:独立解析与敏感内容扫描;
:视觉、交互与隐藏面复核;
:结构、完整性与业务效用校验;

if (关键检查均有证据?) then (是)
  :输出 pass\n审查报告;
else (否)
  if (关键表面无法读取?) then (是)
    :输出 blocked\n审查报告;
  else (否)
    :输出 needs_review\n审查报告;
  endif
endif

stop

@enduml
